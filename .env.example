# Copy to .env (or rely on config/config.php directly)
APP_ENV=production
APP_DEBUG=false
APP_TZ=Asia/Shanghai
DB_PATH=../data/app.db
JWT_SECRET=replace_with_64_byte_random_hex
USERNAME=jian
PASSWORD_HASH=replace_with_php_password_hash
